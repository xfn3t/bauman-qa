spring:
  cloud:
    gateway:
      routes:
        - id: storing-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/works/**
        - id: analysis-service
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/works/*/reports

server:
  port: 8080
