spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/cosmoscan_storing
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin

server:
  port: 8081

analysis:
  service:
    url: http://localhost:8082
